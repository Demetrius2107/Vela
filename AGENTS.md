# Vela 项目编码规范与工作流指令

> 合并自：code-review / comment-style / commit-message / global-codex / im-architecture-deep-dive / session-doc
> 本文件在每次会话开始时自动加载，无需手动触发 skill。

---

## 一、语言与沟通

- 默认使用中文回答，除非用户明确要求其他语言。
- 代码、命令、文件名、API 名称、配置键、日志、错误信息和引用原文保持原语言，不翻译专有名词。
- 技术解释保持直接、准确、简洁；需要给步骤时优先给可执行命令或明确路径。

---

## 二、编码前自检（每次请求默认思考）

### 代码结构
- 函数是否超过 50 行？（超过则考虑拆分）
- 是否有硬编码常量？（抽到常量类或配置文件中）
- 异常被 catch 后是吞掉了还是抛出了/补偿了？
- 注释写了"为什么这么做"而非"做了什么"？

### 增量改动
- 新建了实体？是否有对应的建表 SQL？
- 新增了接口？是否有对应的 REST 端点文档？
- 修改了消息模型？是否同步更新了 `OfflineMessageContent`？

### 架构合规
- 遵循 DDD 分层：interfaces → application → domain ← infrastructure
- 构造器注入，非 @Autowired
- 跨模块引用只允许：interfaces → application → domain ← infrastructure
- 包名符合规范

---

## 三、注释规范

### 类级注释模板
```java
/**
 * <p>Title: ${CLASS_NAME}</p>
 * <p>Description: ${类功能描述}</p>
 * <p>项目名称: Vela</p>
 *
 * @author wanqiu
 * @since ${version}
 * @createTime ${date}
 */
```

### 方法注释模板
```java
/**
 * ${业务逻辑描述，非代码实现细节}
 *
 * @param ${paramName} ${参数说明}
 * @return ${返回值说明}
 */
```

### 字段注释模板
```java
/** ${字段说明} */
private ${type} ${fieldName};
```

### 注意事项
- 方法注释**必须描述业务逻辑**，而非代码实现细节
- DTO/Req/Resp 类字段必须有注释说明
- 枚举类每个枚举值必须有注释
- 保留现有中文注释；新增注释使用英文或中文保持统一

---

## 四、Git 提交规范

### 格式
```
<type>(<scope>): <subject>

[optional body]
```

### Type
- `feat` / `fix` / `refactor` / `docs` / `style` / `test` / `chore`

### Scope
- `im-common` `im-codec` `im-tcp` `im-service` `im-message-store`
- `im-service-user` `im-service-friendship` `im-service-group` `im-service-message` `im-service-conversation`

### 禁止项
- commit message **不允许包含 `Co-Authored-By` 字段**
- 不允许自动附加合作者信息

---

## 五、代码审查检查清单

1. 遵循 DDD 分层架构，职责清晰
2. 包名符合规范
3. 类级注释包含 @description、@author、@version、@date
4. 方法注释包含 @description、@param、@return、@throws
5. 无硬编码常量，使用枚举或常量类
6. 异常处理恰当，使用统一 Result 返回
7. 事务注解 @Transactional 使用正确
8. 依赖注入使用构造器注入（推荐）
9. 跨模块引用只允许：interfaces → application → domain ← infrastructure

---

## 六、工作方法论

### 第一性原理
- 不要先套常见方案。先拆解真正目标、已知输入、约束、不可违反条件、事实与假设。
- 区分事实、推断和假设；对关键假设说明验证方式。
- 先给出最小可行解，再说明更稳健的下一层改进。
- 最终输出必须落到可执行方案：改哪里、怎么改、如何验证、风险是什么。

### 根因解决
- 不要只修表面症状。先分析直接问题、根本原因、现有设计为什么允许问题发生。
- 明确哪些临时补丁不推荐，以及它们为什么会留下隐患。
- 优先给出根因级解决方案；只有在时间、风险或兼容性限制下，才采用临时方案，并说明后续清理路径。

### 挑战用户思路
- 不要默认用户判断正确。把用户给出的方案当作假设审查。
- 明确列出隐含假设、最容易错的假设、最可能失败的场景和替代方案。
- 如果用户方案与目标、验收标准或代码事实冲突，以目标和事实为准，并直接说明冲突。
- 从负责人视角给出选择建议：兼顾正确性、维护成本、风险、交付速度和可回滚性。

### 先理解代码再改
- 修改前先定位相关文件、调用链、数据流和已有测试。
- 解释当前逻辑如何工作，判断设计为什么这样写，再找最小必要改动点。
- 开始实现前说明改动范围、主要风险和验证方式。
- 优先复用现有模式、类型、工具函数和模块边界；避免无关重构。

### 信任 AI 自主探索
- 用户给出目标、验收标准和约束后，允许 AI 自主阅读代码、搜索上下文、运行测试、调整实现方案。
- 如果用户给的路径或方案与目标冲突，以目标和验收标准为准；先说明冲突，再选择更合理的实现路径。
- 遇到不确定点，优先通过代码、文档、测试和工具验证；只有无法可靠判断且风险较高时才向用户确认。
- 完成后说明改了什么、为什么这样改、如何验证，以及仍然存在的风险或后续建议。

---

## 七、IM 架构深度设计 — 自检维度（参考）

当涉及 IM 核心模块深度设计时，按以下层次逐层思考：

```
第一层：能跑通（主流程）
第二层：能扛异常（边界条件全覆盖）
第三层：能扛并发（性能优化）
第四层：能扛故障（容灾降级自愈）
第五层：能扛面试（决策论证，能讲30分钟）
```

### 代码质量演进
| 阶段 | 特征 | 目标 |
|------|------|------|
| 现在 | 满屏 if-else | 理解问题 |
| 下一步 | 策略模式 + 管道 + 状态机 | 隔离复杂度 |
| 最终 | 配置化 + 规则引擎 + 数据驱动 | 运营自助 |

---

## 八、会话工作记录（每次会话结束时执行）

1. 生成文档到 `docs/会议记录/YYYY-MM-DD-<主题>.md`
2. 文档包含：完成工作/文件清单/Git 提交记录/待办事项
3. 按模板输出后 `git add` 并 `git commit -m "docs: 会话工作记录——<主题>"`
