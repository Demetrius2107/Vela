-- ============================================================================
-- Vela IM — V1 初始数据库基线
-- 说明：Flyway 基线迁移，标记已有数据库结构的起点。
-- 后续所有表结构变更都通过 V2__xxx.sql / V3__xxx.sql 管理。
-- 实际建表语句参见 docs/MySQL/vela-study.sql
-- ============================================================================

-- flyway baseline 会自动跳过此文件（已有表），只为记录版本基线
-- 所有 V1 之后的迁移才真正执行 DDL/DML
SELECT 1;
