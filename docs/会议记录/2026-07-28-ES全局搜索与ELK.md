# Vela IM — 会话工作记录

> 日期：2026-07-28
> 分支：feat/message-reply
> 状态：开发进行中

---

## 本次完成工作

### 1. Elasticsearch 全局搜索

| 文件 | 说明 |
|------|------|
| `pom.xml` | 新增 `spring-boot-starter-data-elasticsearch` 依赖 |
| `application.yml` | ES 连接配置（localhost:9200）|
| `MessageDocument.java` | ES 索引文档映射（ik_smart 中文分词）|
| `MessageSearchRepository.java` | ElasticsearchRepository 数据访问层 |
| `MessageIndexService.java` | 消息存储时自动索引到 ES |
| `MessageSearchService.java` | ES 全文搜索（matchQuery + termQuery 过滤）|
| `MessageStoreService.java` | `storeP2PMessage` 和 `storeOfflineMessage` 后自动索引 |
| `AdminService.java` | `searchMessages` 方法：有关键词走 ES，无关键词降级 SQL |
| `docker-compose.yml` | 新增 ES 7.17.22 + Kibana + Logstash 服务 |

### 2. ELK 日志体系

| 文件 | 说明 |
|------|------|
| `deploy/logstash/logstash.conf` | Logstash 管道：beats/tcp 输入 → ES 输出 |
| `docker-compose.yml` | Logstash 服务（5044/5000 端口，挂载配置）|
| `es-data` 持久化卷 | ES 数据持久化 |

### 3. 管理后台补全

侧边栏新增 3 个功能页面：

| 页面 | 功能 |
|------|------|
| 📋 操作日志 | 自动记录全部管理操作 + 查询 |
| 🔐 管理员 | 超管/运营/审计三级权限 |
| ⚙️ 系统配置 | 动态参数调整 |

## Git 提交记录

```
30e1480 feat(es,elk): Elasticsearch全局搜索+ELK日志体系
```

---

> 文档版本: v1.0 | 创建时间: 2026-07-28
