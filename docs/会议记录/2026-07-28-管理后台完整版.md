# Vela IM — 会话工作记录

> 日期：2026-07-28
> 分支：feat/message-reply
> 状态：开发进行中

---

## 本次完成工作

### 1. 系统指令整合

| 文件 | 说明 |
|------|------|
| `AGENTS.md` | **新建** — 合并全部 6 个 skills 的编码规范与工作流指令，8 个章节 |
| `.atomcode/skills/code-review.md` | 追加编码前自检清单（函数行数/硬编码/异常处理/注释质量） |

### 2. 管理后台后端（完整版）

| 模块 | 端点 | 说明 |
|------|------|------|
| 看板 | `GET /v1/admin/dashboard` | 5 项统计（用户/群组/消息/活跃群/禁用用户） |
| 趋势 | `GET /v1/admin/message/trend` | 消息趋势分析（指定天数/总量/日均） |
| TOP排行 | `GET /v1/admin/groups/top` | 最活跃群组排行 |
| 用户列表 | `GET /v1/admin/users` | 分页+关键词搜索 |
| 用户详情 | `GET /v1/admin/users/detail` | 用户全部字段 |
| 编辑用户 | `POST /v1/admin/users/update` | 昵称/性别/签名/地区 |
| 禁用/解封 | `POST /v1/admin/users/toggleForbidden` | 切换禁用状态 |
| 批量禁用 | `POST /v1/admin/users/batchForbidden` | 批量操作 |
| 登录日志 | `GET /v1/admin/users/loginLogs` | 按用户查询登录记录 |
| 群组列表 | `GET /v1/admin/groups` | 分页+状态筛选+关键词 |
| 群组详情 | `GET /v1/admin/groups/detail` | 群组全部字段 |
| 解散群组 | `POST /v1/admin/groups/dissolve` | 更新状态为已解散 |
| 消息搜索 | `GET /v1/admin/messages/search` | 关键词+用户+分页 |

### 3. 管理后台前端

| 页面 | 功能 |
|------|------|
| 数据看板 | 5 张统计卡片 + 消息趋势 + Top 10 活跃群组 |
| 用户管理 | 列表/搜索/分页/多选/批量禁用/详情抽屉/单条禁用 |
| 群组管理 | 列表/状态筛选/搜索/详情抽屉/解散 |
| 消息审计 | 关键词搜索 + 按用户筛选 + 分页 |

### 4. 新增文件清单（后端）

```
vela-service/src/main/java/com/vela/im/service/admin/
├── domain/
│   ├── AdminLoginLogEntity.java
│   └── service/AdminService.java
├── infrastructure/persistence/mapper/
│   └── AdminLoginLogMapper.java
└── rest/AdminController.java
```

## Git 提交记录

```
10f079f feat(im-service,web): 完整管理后台——用户/群组/审计/看板
```

---

> 文档版本: v1.0 | 创建时间: 2026-07-28
