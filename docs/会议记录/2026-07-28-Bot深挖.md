# Vela IM — 会话工作记录

> 日期：2026-07-28
> 分支：feat/message-reply
> 状态：开发进行中

---

## 本次完成工作

### Bot 深挖——内联键盘 + 行内查询

| 文件 | 说明 |
|------|------|
| `MessageCommand.java` | 新增 `CALLBACK_QUERY(0x910)` / `INLINE_QUERY(0x911)` 指令 |
| `BotMessageRelayService.java` | `processWebhookResponse` 解析 buttons 按钮；`sendReply` 支持 buttons 参数（存入 `extra` 字段）；新增 `handleCallbackQuery` 按钮回调查询转发 |
| `InlineQueryService.java` | **新建** — 行内查询服务，转发 `@bot query` 到 Webhook，返回建议列表 |
| `InlineQueryController.java` | **新建** — `GET /v1/bot/inline/query` 行内查询 REST 端点 |

## Git 提交记录

```
b111482 feat(bot): Bot深挖——内联键盘+回调查询+行内查询
```

---

> 文档版本: v1.0 | 创建时间: 2026-07-28
